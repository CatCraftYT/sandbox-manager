name: Test Application

inherit:
  - base
  - base-electron
  - network
  - wayland-socket
  - pulseaudio
  - pipewire
  - fakehome
  - xdg-portal-workaround

permissions:
  dbus:
    talk:
      - org.freedesktop.portal.*

run: zsh
